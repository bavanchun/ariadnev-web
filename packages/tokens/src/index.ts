// Package boundary placeholder.
//
// Phase 5 owns the token implementation and the generated `tokens.css`. This
// file exists so the package compiles and its exports resolve today, which is
// what lets Phase 5 land without touching the shared manifest or lockfile.
export const TOKENS_PACKAGE_READY = true;
