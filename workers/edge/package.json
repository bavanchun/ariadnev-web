{
  "name": "@vcskill-web/edge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Release edge Worker extracted in Phase 3. Owns the protected install, version, and download routes.",
  "scripts": {
    "build": "wrangler deploy --dry-run --config wrangler.combined.toml",
    "check": "wrangler deploy --dry-run --config wrangler.edge.toml",
    "test": "node --test src/index.test.mjs",
    "deploy:staging": "wrangler deploy --config wrangler.combined.toml"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260810.1",
    "wrangler": "4.120.0"
  }
}
