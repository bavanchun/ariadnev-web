{
  "name": "@vcskill-web/bridge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Interim release bridge Worker owning ariadnev.com until the candidate-b cutover retires it.",
  "scripts": {
    "check": "wrangler deploy --dry-run --config wrangler.toml",
    "test": "node --test src/index.test.mjs",
    "deploy": "wrangler deploy --config wrangler.toml"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260810.1",
    "wrangler": "4.120.0"
  }
}
